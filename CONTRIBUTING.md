# Contributing

1. Open an issue or proposal for behavior changes.
2. Add or update schemas before changing durable data.
3. Add tests for every new capability.
4. Preserve unknown fields and native project bytes.
5. Do not add a UI action or agent tool that bypasses the operation layer.
6. Sign commits using the Developer Certificate of Origin (`git commit -s`).
